[pytest]
minversion = 8.0
addopts = -ra -q --strict-markers
testpaths = tests
pythonpath = .
markers =
    unit: fast, no external calls (indicators, ensemble logic)
    integration: hits DB or FastAPI TestClient
    slow: loads FinBERT or hits real yfinance