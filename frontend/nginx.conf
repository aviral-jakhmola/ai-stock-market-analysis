server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # React Router (client-side routing) — any path not matching a real
    # file falls back to index.html, letting React Router handle it
    location / {
        try_files $uri $uri/ /index.html;
    }
}